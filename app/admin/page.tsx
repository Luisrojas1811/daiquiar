'use client'

import { FormEvent, useState } from 'react'
import { ArrowLeft, LockKeyhole, Plus, Trash2 } from 'lucide-react'

type AdminProduct = { id: number; name: string; category: string; price: number; image: string; stock: number; lowStockThreshold: number }

const demoProducts: AdminProduct[] = [
  { id: 1, name: 'Body Negro Escote V', category: 'Bodies', price: 24990, image: 'https://images.unsplash.com/photo-1596755389378-c31d21fd1273?auto=format&fit=crop&w=180&q=80', stock: 2, lowStockThreshold: 3 },
  { id: 2, name: 'Catsuit Negro Manga Larga', category: 'Catsuits', price: 38990, image: 'https://images.unsplash.com/photo-1551488831-00ddcb6c6bd3?auto=format&fit=crop&w=180&q=80', stock: 8, lowStockThreshold: 3 },
  { id: 3, name: 'Musculosa Básica Bordó', category: 'Conjuntos', price: 16990, image: 'https://images.unsplash.com/photo-1564257577054-3e2b7c1b5f3b?auto=format&fit=crop&w=180&q=80', stock: 5, lowStockThreshold: 3 },
  { id: 4, name: 'Conjunto Cut Out', category: 'Conjuntos', price: 42990, image: 'https://images.unsplash.com/photo-1506629905607-d9f3bb9f7b36?auto=format&fit=crop&w=180&q=80', stock: 2, lowStockThreshold: 3 },
  { id: 5, name: 'Body Manga Larga', category: 'Bodies', price: 27990, image: 'https://images.unsplash.com/photo-1618244972963-dbee1a7edc95?auto=format&fit=crop&w=180&q=80', stock: 8, lowStockThreshold: 3 },
  { id: 6, name: 'Top Corset Bordó', category: 'Conjuntos', price: 22990, image: 'https://images.unsplash.com/photo-1605763240000-7e93b172d754?auto=format&fit=crop&w=180&q=80', stock: 2, lowStockThreshold: 3 },
]

export default function AdminPage() {
  const [logged, setLogged] = useState(false)
  const [products, setProducts] = useState(demoProducts)
  const [categories, setCategories] = useState(['Bodies', 'Catsuits', 'Conjuntos'])
  const [message, setMessage] = useState('')
  const [copy, setCopy] = useState({ title: 'Moda que inspira tu estilo.', description: 'Indumentaria femenina seleccionada con amor. Diseños únicos, calidad y envíos a todo el país.', footer: 'HEY BBY! Somos Daiquiar. Diseño y calidad a precios únicos. CABA, Buenos Aires. Envíos a todo el país.', whatsapp: '5491100000000', shippingThreshold: 75000, correoArgentinoUrl: 'https://www.correoargentino.com.ar/formularios/cpa', benefits: [{ title: 'Envío gratis', description: 'En compras superiores a $75.000' }, { title: 'Compra simple', description: 'Coordinamos tu pedido por WhatsApp' }, { title: 'Atención cercana', description: 'Te ayudamos a elegir tu look' }] })

  function saveCopy(event: FormEvent<HTMLFormElement>) {
    event.preventDefault()
    const data = new FormData(event.currentTarget)
    const next = { title: String(data.get('title')), description: String(data.get('description')), footer: String(data.get('footer')), whatsapp: String(data.get('whatsapp')).replace(/\D/g, ''), shippingThreshold: Math.max(0, Number(data.get('shippingThreshold')) || 0), correoArgentinoUrl: String(data.get('correoArgentinoUrl') || 'https://www.correoargentino.com.ar/formularios/cpa'), benefits: [0, 1, 2].map(index => ({ title: String(data.get(`benefitTitle${index}`)), description: String(data.get(`benefitDescription${index}`)) })) }
    setCopy(next)
    window.localStorage.setItem('daiquiar-copy', JSON.stringify(next))
    setMessage('Textos actualizados en la tienda pública')
  }

  function login(event: FormEvent<HTMLFormElement>) {
    event.preventDefault()
    setLogged(true)
  }

  function addCategory(event: FormEvent<HTMLFormElement>) { event.preventDefault(); const data = new FormData(event.currentTarget); const name = String(data.get('categoryName')).trim(); if (name && !categories.includes(name)) setCategories(current => [...current, name]); event.currentTarget.reset(); setMessage('Categoría agregada al catálogo') }

  function updateStock(event: FormEvent<HTMLFormElement>) { event.preventDefault(); const data = new FormData(event.currentTarget); const id = Number(data.get('productId')); const stock = Math.max(0, Number(data.get('stock'))); setProducts(current => current.map(product => product.id === id ? { ...product, stock } : product)); setMessage('Stock actualizado'); }

  function updateAllStock(event: FormEvent<HTMLFormElement>) { event.preventDefault(); const data = new FormData(event.currentTarget); setProducts(current => current.map(product => ({ ...product, stock: Math.max(0, Number(data.get(`stock-${product.id}`)) || 0) }))); setMessage('Stock de todos los productos actualizado'); }

  function addProduct(event: FormEvent<HTMLFormElement>) {
    event.preventDefault()
    const data = new FormData(event.currentTarget)
    setProducts(current => [{
      id: Date.now(), name: String(data.get('name')), category: String(data.get('category')), price: Number(data.get('price')), image: String(data.get('image')) || demoProducts[0].image, stock: Math.max(0, Number(data.get('stock')) || 0), lowStockThreshold: Math.max(1, Number(data.get('lowStockThreshold')) || 3),
    }, ...current])
    event.currentTarget.reset()
    setMessage('Producto agregado al catálogo de esta demo')
  }

  return <main className="min-h-screen bg-background text-foreground">
    <header className="border-b border-border bg-card"><div className="mx-auto flex max-w-5xl items-center justify-between px-5 py-5"><a href="/" className="flex items-center gap-2 text-sm font-semibold text-primary"><ArrowLeft size={17}/> Ver tienda pública</a><span className="font-serif text-2xl font-bold">DAIQUIAR</span><span className="text-xs uppercase tracking-widest text-muted-foreground">Panel privado</span></div></header>
    <div className="mx-auto max-w-5xl px-5 py-12">{!logged ? <section className="mx-auto max-w-md rounded-2xl border border-border bg-card p-8 shadow-sm"><div className="mb-6 flex h-12 w-12 items-center justify-center rounded-full bg-primary text-primary-foreground"><LockKeyhole size={21}/></div><p className="text-xs font-semibold uppercase tracking-[0.2em] text-primary">Daiquiar Indumentaria</p><h1 className="mt-2 font-serif text-4xl font-bold">Ingresar al panel</h1><p className="mt-3 text-sm leading-6 text-muted-foreground">Este enlace es privado y no aparece en la tienda de clientes.</p><form onSubmit={login} className="mt-7 space-y-4"><label className="block text-sm font-medium">Email<input required type="email" placeholder="tu@email.com" className="mt-2 w-full rounded-xl border border-input bg-background px-4 py-3 outline-none"/></label><label className="block text-sm font-medium">Contraseña<input required type="password" placeholder="••••••••" className="mt-2 w-full rounded-xl border border-input bg-background px-4 py-3 outline-none"/></label><button className="w-full rounded-xl bg-primary py-3 font-semibold text-primary-foreground">Entrar</button></form><p className="mt-5 text-center text-xs text-muted-foreground">Demo: cualquier email y contraseña.</p></section> : <><div className="mb-8 flex flex-col justify-between gap-4 md:flex-row md:items-end"><div><p className="text-xs font-semibold uppercase tracking-[0.2em] text-primary">Administración</p><h1 className="mt-2 font-serif text-4xl font-bold">Catálogo de productos</h1><p className="mt-2 text-sm text-muted-foreground">Los cambios son de demostración y todavía no persisten.</p></div><a href="/" className="text-sm font-semibold text-primary">Abrir tienda pública</a></div><form onSubmit={saveCopy} className="mb-8 rounded-2xl border border-border bg-card p-6"><h2 className="font-bold">Contacto, textos y envíos</h2><p className="mt-2 text-sm text-muted-foreground">Configurá desde acá el monto mínimo y la URL de Correo Argentino. Al superar el monto, en el carrito se muestra únicamente esa opción. También podés editar cuándo ofrecer envío por Correo Argentino y editá la información pública.</p><div className="mt-4 grid gap-4"><label className="text-sm font-medium">Envío por Correo Argentino desde<input required name="shippingThreshold" type="number" min="0" step="1" defaultValue="75000" placeholder="Monto mínimo de compra" className="mt-2 w-full rounded-xl border border-input bg-background px-4 py-3 outline-none"/><span className="mt-1 block text-xs text-muted-foreground">En compras menores, el pedido se coordina directamente por WhatsApp.</span></label><label className="text-sm font-medium">URL de Correo Argentino<input required name="correoArgentinoUrl" type="url" defaultValue="https://www.correoargentino.com.ar/formularios/cpa" className="mt-2 w-full rounded-xl border border-input bg-background px-4 py-3 outline-none"/><span className="mt-1 block text-xs text-muted-foreground">Se abre cuando la clienta elige esa opción de envío.</span></label><label className="text-sm font-medium">WhatsApp<input required name="whatsapp" defaultValue={copy.whatsapp} placeholder="5491100000000" className="mt-2 w-full rounded-xl border border-input bg-background px-4 py-3"/></label><label className="text-sm font-medium">Título principal<input required name="title" defaultValue={copy.title} className="mt-2 w-full rounded-xl border border-input bg-background px-4 py-3"/></label><label className="text-sm font-medium">Descripción<textarea required name="description" defaultValue={copy.description} className="mt-2 min-h-20 w-full rounded-xl border border-input bg-background px-4 py-3"/></label>{copy.benefits.map((benefit, index) => <div key={index} className="grid gap-3 rounded-xl border border-border p-4"><p className="text-sm font-semibold">Beneficio {index + 1}</p><input required name={`benefitTitle${index}`} defaultValue={benefit.title} placeholder="Título" className="rounded-xl border border-input bg-background px-4 py-3"/><input required name={`benefitDescription${index}`} defaultValue={benefit.description} placeholder="Descripción" className="rounded-xl border border-input bg-background px-4 py-3"/></div>)}<label className="text-sm font-medium">Pie de página<textarea required name="footer" defaultValue={copy.footer} className="mt-2 min-h-20 w-full rounded-xl border border-input bg-background px-4 py-3"/></label><button className="rounded-xl bg-primary px-5 py-3 font-semibold text-primary-foreground">Guardar cambios</button></div></form><form onSubmit={saveCopy} className="mb-8 rounded-2xl border border-border bg-card p-6"><h2 className="font-bold">Textos de la tienda</h2><p className="mt-1 text-sm text-muted-foreground">Editá el mensaje principal y la descripción sin tocar el código.</p><div className="mt-4 grid gap-3"><label className="text-sm font-medium">Título principal<input name="title" defaultValue={copy.title} required className="mt-2 w-full rounded-xl border border-input bg-background px-4 py-3"/></label><label className="text-sm font-medium">Descripción del inicio<textarea name="description" defaultValue={copy.description} required className="mt-2 min-h-24 w-full rounded-xl border border-input bg-background px-4 py-3"/></label><label className="text-sm font-medium">Texto del pie de página<textarea name="footer" defaultValue={copy.footer} required className="mt-2 min-h-24 w-full rounded-xl border border-input bg-background px-4 py-3"/></label><button className="w-fit rounded-xl bg-primary px-5 py-3 font-semibold text-primary-foreground">Guardar textos</button></div></form><div className="mb-8 rounded-2xl border border-border bg-card p-6"><div className="flex flex-col justify-between gap-4 sm:flex-row sm:items-center"><div><h2 className="font-bold">Categorías</h2><p className="mt-1 text-sm text-muted-foreground">Agregá las categorías que quieras mostrar en la tienda.</p></div><form onSubmit={addCategory} className="flex gap-2"><input required name="categoryName" placeholder="Nueva categoría" className="rounded-xl border border-input bg-background px-4 py-3"/><button className="rounded-xl bg-primary px-4 py-3 font-semibold text-primary-foreground"><Plus size={18}/></button></form></div><div className="mt-4 flex flex-wrap gap-2">{categories.map(category => <span key={category} className="rounded-full bg-muted px-3 py-1 text-sm">{category}</span>)}</div></div><div className="mb-8 rounded-2xl border border-border bg-card p-6"><h2 className="font-bold">Stock e inventario</h2><p className="mt-1 text-sm text-muted-foreground">Actualizá cuántas unidades quedan. Cuando el stock sea menor o igual a 3, la tienda mostrará “Quedan X unidades”.</p><form onSubmit={updateStock} className="mt-4 flex flex-wrap gap-3"><select name="productId" className="rounded-xl border border-input bg-background px-4 py-3">{products.map(product => <option key={product.id} value={product.id}>{product.name} · quedan {product.stock}</option>)}</select><input required name="stock" type="number" min="0" placeholder="Unidades disponibles" className="w-48 rounded-xl border border-input bg-background px-4 py-3"/><button className="rounded-xl bg-primary px-5 py-3 font-semibold text-primary-foreground">Actualizar stock</button></form></div><form onSubmit={updateAllStock} className="mb-8 rounded-2xl border border-border bg-card p-6"><div className="flex flex-col justify-between gap-2 sm:flex-row sm:items-center"><div><h2 className="font-bold">Inventario completo</h2><p className="mt-1 text-sm text-muted-foreground">Revisá y modificá el stock de todos los productos desde un solo lugar.</p></div><button className="rounded-xl bg-primary px-5 py-3 font-semibold text-primary-foreground">Guardar todo el stock</button></div><div className="mt-5 space-y-3">{products.map(product => <div key={product.id} className="flex flex-col gap-3 rounded-xl border border-border p-4 sm:flex-row sm:items-center"><img src={product.image} alt="" className="h-14 w-14 rounded-lg object-cover"/><div className="min-w-0 flex-1"><p className="truncate font-semibold">{product.name}</p><p className="text-sm text-muted-foreground">{product.category} · ${product.price.toFixed(2)}</p></div><label className="text-sm font-medium">Unidades<input name={`stock-${product.id}`} type="number" min="0" defaultValue={product.stock} className="mt-1 w-full rounded-lg border border-input bg-background px-3 py-2 sm:w-28"/></label><span className={`text-sm font-semibold ${product.stock === 0 ? 'text-muted-foreground' : product.stock <= product.lowStockThreshold ? 'text-primary' : 'text-emerald-600'}`}>{product.stock === 0 ? 'Agotado' : product.stock <= product.lowStockThreshold ? `Quedan ${product.stock}` : 'Disponible'}</span></div>)}</div></form><div className="grid gap-8 lg:grid-cols-[0.8fr_1.2fr]"><form onSubmit={addProduct} className="h-fit rounded-2xl border border-border bg-card p-6"><h2 className="flex items-center gap-2 font-bold"><Plus size={18}/> Nuevo producto</h2><div className="mt-5 space-y-3"><input required name="name" placeholder="Nombre" className="w-full rounded-xl border border-input bg-background px-4 py-3"/><select name="category" className="w-full rounded-xl border border-input bg-background px-4 py-3"><option>Bodies</option><option>Conjuntos</option><option>Básicos</option></select><input required name="price" type="number" placeholder="Precio" className="w-full rounded-xl border border-input bg-background px-4 py-3"/><input name="image" placeholder="URL de imagen" className="w-full rounded-xl border border-input bg-background px-4 py-3"/><button className="w-full rounded-xl bg-primary py-3 font-semibold text-primary-foreground">Agregar producto</button></div>{message && <p className="mt-4 text-sm text-primary">{message}</p>}</form><section className="rounded-2xl border border-border bg-card p-6"><div className="flex items-center justify-between"><h2 className="font-bold">Productos activos</h2><span className="rounded-full bg-muted px-3 py-1 text-xs">{products.length}</span></div><div className="mt-5 divide-y divide-border">{products.map(product => <div key={product.id} className="flex items-center gap-4 py-4"><img src={product.image} alt="" className="h-16 w-16 rounded-xl object-cover"/><div className="min-w-0 flex-1"><p className="font-semibold">{product.name}</p><p className="text-sm text-muted-foreground">{product.category} · ${product.price.toLocaleString('es-AR')}</p></div><button aria-label={`Eliminar ${product.name}`} onClick={() => setProducts(current => current.filter(item => item.id !== product.id))} className="text-muted-foreground hover:text-destructive"><Trash2 size={18}/></button></div>)}</div></section></div></>}</div>
  </main>
}
